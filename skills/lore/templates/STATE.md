# STATE — <project name>

> Read this first, every session. Keep under ~150 lines: this is a dashboard of *now*,
> not an archive. Anything that becomes past tense moves to JOURNAL / DECISIONS / LEARNINGS.

**Last updated:** <YYYY-MM-DD>

## What this project is

<2–4 lines. The goal, and who/what it is for. Not a feature list.>

## Stack & entry points

- **Run:** `<command>` → `<url or output>`
- **Test:** `<command>`
- **Key files:** `<path>` — <one-line role>

## Works right now

- [x] <capability that is done and verified — say how it was verified>

## In progress

- [ ] <what is half-built, and precisely where it stopped>
  - Files touched: `<paths>`
  - Stopped because: <reason>

## Blockers

- **<blocker>** — <what is blocked, what would unblock it, who/what it waits on>

## Next steps

> Mark every step: `▶️` = actionable now · `⛔ [needs user]` = blocked on the user
> (a credential, a decision, a judgement call, something only they can run or see).
> A session picks the first `▶️` and starts; `⛔` items are reported as reminders, not asked about.

1. ⛔ [needs user] <what only they can do, and what it unblocks>
2. ▶️ <the first thing a session can actually start on>
3. ▶️ <then this>

## Gotchas to remember right now

<Short-lived warnings about the current working state — "the dev server must be restarted
after editing server/src". Durable lessons belong in LEARNINGS.md instead.>
