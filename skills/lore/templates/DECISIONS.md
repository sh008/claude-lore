# DECISIONS — <project name>

> Why the project is the way it is. Newest first.
> Only decisions with a real alternative belong here — if there was no other option,
> it was not a decision, it was a constraint (those go in LEARNINGS.md).
>
> When a decision is later reversed, do not delete it: add a new entry that
> supersedes it and link back. The reversal is itself the valuable record.

---

## <YYYY-MM-DD> — <decision in one line>

**Chose:** <what>

**Why:** <the reason that actually drove it — cost, a measurement, a constraint>

**Rejected:** <alternative> — <why not. This line is the one a future session needs.>

**Affects:** `<files or subsystems>`

**Revisit if:** <the condition that would make this the wrong call — e.g. "if the
dataset passes ~1M rows". Leave out if there isn't one.>

---
